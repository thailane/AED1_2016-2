#include <stdio.h>
#include <stdlib.h>

int main(void){
	int *v
	int num;
	int i, k, aux;

	v = (int*)malloc(5 * sizeof(int));

	for (i=0;i<5;i++)
	{
		printf("Digite um valor para V[%d]: ",i);
		scanf("%d",v+i);

	}
	
}
