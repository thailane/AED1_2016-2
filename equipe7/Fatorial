#include <stdio.h>
#include <stdlib.h>

int fatorial(int n)
{
  int m;
  
  m=n*fatorial(n-1);
  
  return m;
}

int main ()
{
  int n, r;
  scanf("%d", &n);
  
  r=fatorial(n)
  printf("%d", r);
  return 0;
}
